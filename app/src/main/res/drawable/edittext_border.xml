<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Fundo branco -->
    <solid android:color="@android:color/white" />

    <!-- Borda mais escura -->
    <stroke
        android:width="2dp"
        android:color="#555555" /> <!-- cinza escuro -->

    <!-- Cantos arredondados -->
    <corners android:radius="8dp" />
</shape>
